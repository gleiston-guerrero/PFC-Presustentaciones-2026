# Archivos generados y carpetas de compilación
target/
dist/
build/
out-tsc/
bazel-out/
tmp/
.tmp/

# Dependencias de Node (Angular)
node_modules/
npm-debug.log
yarn-error.log
.angular/

# Archivos comprimidos
*.zip
*.rar
*.tar
*.gz
*.7z
# Excepcion: evidencia cruda de k6 (NDJSON por peticion), comprimida para no versionar 21.9 MB sin comprimir
!k6/run8-raw.ndjson.gz

# Spring Boot / Maven
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

# IDEs y Editores
.idea/
*.iws
*.iml
*.ipr
.vscode/
.claude/

# Logs y otros
*.log

# Archivos subidos en tiempo de ejecución (anteproyectos, fases de tutoría)
backend/uploads/

# Variables de entorno con secretos reales (usar .env.example como plantilla)
.env
backend/.env

# Salida local de Lighthouse CI (lhci autorun / lhci collect)
.lighthouseci/

# Artefactos de compilacion LaTeX (se regeneran con `make pdf` / latexmk)
*.aux
*.fdb_latexmk
*.fls
*.lof
*.lot
*.lol
*.out
*.toc
*.synctex.gz

# Bytecode de Python: se genera al importar los scripts, no es fuente
__pycache__/
*.pyc
