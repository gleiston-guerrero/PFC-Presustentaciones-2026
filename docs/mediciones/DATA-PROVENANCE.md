# Procedencia de datos (Data Provenance)

**Propósito:** para cada tabla o figura con datos empíricos citada en el informe final ([`Informe-Final/informe-final.tex`](../../Informe-Final/informe-final.tex), particularmente [`10-evaluacion-empirica.tex`](../../Informe-Final/secciones/10-evaluacion-empirica.tex)), en `README.md`, o en los documentos de `docs/mediciones/`, este archivo declara explícitamente **qué archivo crudo la origina** y **qué comando/script la produjo**, para que cualquier cifra citada sea reproducible y verificable en lugar de tener que confiarse "de palabra". Coincide con el principio de "el repositorio Git es la fuente de verdad" — si un número no puede rastrearse hasta un archivo crudo en esta tabla, no debería citarse en el informe. (El informe técnico anterior, `Informe-UNIDAD-4-PRESUS/`, se conserva como histórico — ver `README.md` — pero el informe vigente es `Informe-Final/`.)

| Dato / cifra citada | Dónde se cita | Archivo(s) crudo(s) de origen | Comando/script que lo produjo |
|---|---|---|---|
| Cobertura JaCoCo (instrucciones 35.10%, líneas 38.88%, ramas 23.06%, 109 tests — actualizado 2026-08-30 tras el fix de `PreSustentacionesApplicationTests`) | `README.md` (badge), [`jacoco/COVERAGE.md`](jacoco/COVERAGE.md) | [`jacoco/2026-08-30/jacoco.csv`](jacoco/2026-08-30/jacoco.csv) (snapshot archivado; se regenera en `backend/target/site/jacoco/jacoco.csv`, no versionado) | `cd backend && ./mvnw test` (JaCoCo corre en la fase `test`, plugin configurado en `backend/pom.xml`) |
| Resultados de pruebas unitarias (109/109 tests, 0 fallos — actualizado 2026-08-30, incluye ahora un `@SpringBootTest` real que migra Flyway contra Postgres/Redis reales, no un `assertTrue(true)`) | `README.md`, este repositorio (CI) | `backend/target/surefire-reports/*.txt` (no versionado; publicado como artefacto en CI) | `./mvnw test`, o el job `backend` de [`../../.github/workflows/ci.yml`](../../.github/workflows/ci.yml) |
| Bug real de reproducibilidad en `V1__schema_inicial.sql` (secuencias `roles_usuario_seq`/`modalidades_titulacion_seq` sin cualificar esquema, rompía una migración limpia en V14) — detectado y corregido 2026-08-30 | `README.md` (sección R1), `V1__schema_inicial.sql` | Log de fallo original y de verificación posterior (no versionados; reproducible corriendo el comando de la derecha contra una BD Postgres recién creada) | `./mvnw -Dtest=PreSustentacionesApplicationTests -Dspring.datasource.url=jdbc:postgresql://localhost:5432/<db_vacia> ... test` (ver README para el comando completo con todas las `-D`) |
| 5 corridas de carga k6 válidas (p95, avg, http_req_failed por corrida) | [`perf/README.md`](perf/README.md), `k6/README.md` | [`../../k6/run3-summary.json`](../../k6/run3-summary.json) … [`run7-summary.json`](../../k6/run7-summary.json) (`run1`/`run2` se conservan como evidencia de una corrida inválida, no se usan en las estadísticas) | `k6 run --quiet --summary-export=runN-summary.json k6/load-test.js` (script en [`../../k6/load-test.js`](../../k6/load-test.js)) |
| Evidencia de rate limiting bajo carga (99.8% de fallos) | `k6/README.md` | [`../../k6/rate-limiter-evidence-run.json`](../../k6/rate-limiter-evidence-run.json) | Misma corrida k6, con la metodología antigua (login por iteración) que ya no se usa; conservada como evidencia |
| Percentiles p50/p90/p95/p99, media, IC 95% y familia de 3 pruebas (Mann-Whitney U + permutación de mediana + permutación de p95) con corrección de Holm-Bonferroni (caché fría vs. caliente — P6, actualizado 2026-09-16) | `k6/README.md`, [`10-evaluacion-empirica.tex`](../../Informe-Final/secciones/10-evaluacion-empirica.tex) | [`../../k6/cache-cold-samples.txt`](../../k6/cache-cold-samples.txt), [`cache-warm-samples.txt`](../../k6/cache-warm-samples.txt) | Muestreo: ver comandos `curl`/`redis-cli` documentados en `k6/README.md`. Cálculo estadístico reproducible en [`../../scripts/perf-analysis.ipynb`](../../scripts/perf-analysis.ipynb) (Mann-Whitney U vía `scipy.stats`, tests de permutación de mediana/p95 con $10^5$ repeticiones y semilla fija, corrección de Holm implementada explícitamente en el propio cuaderno) |
| 6 corridas de Lighthouse (Performance/Accessibility/Best Practices/SEO, 3 desktop + 3 mobile — actualizado 2026-08-30, Accessibility 89→100) | `README.md`, [`perf/lighthouse/LIGHTHOUSE-REPORT.md`](perf/lighthouse/LIGHTHOUSE-REPORT.md) | [`perf/lighthouse/prod-runs/desktop-run1.json`](perf/lighthouse/prod-runs/desktop-run1.json) … `mobile-run3.json` (corrida 17-08 conservada en [`prod-runs/2026-08-17-PREVIOUS/`](perf/lighthouse/prod-runs/2026-08-17-PREVIOUS/)) | `npx lighthouse http://localhost:4300/ --preset=desktop\|(default=mobile) --output=json --output-path=...`, contra `ng build --configuration production` servido con `http-server` — comandos completos en el reporte. Config reproducible en [`../../lighthouserc.js`](../../lighthouserc.js) (`npx @lhci/cli autorun`) |
| Reducción de bundle inicial (1.01 MB → 396.21 KB, −61% — actualizado 2026-08-30; la cifra "387 KB" citada hasta el 29-08 quedó desactualizada tras el bump de `@angular/core` 21.2.12→21.2.22) | Conversación de sesión, commits de `Frontend/src/app/app.routes.ts` | Salida de `npx ng build --configuration production` (sección "Initial chunk files") | `cd Frontend && npx ng build --configuration production` |
| Escaneo OWASP ZAP (0 FAIL, 3 WARN, 0 High — re-corrido 2026-08-29 tras corregir `10003`) | [`sec/owasp/OWASP-AUDIT.md`](sec/owasp/OWASP-AUDIT.md) | [`sec/zap/zap-baseline-report.json`](sec/zap/zap-baseline-report.json), [`zap-baseline-report.html`](sec/zap/zap-baseline-report.html) (corrida 17-08 conservada como `*.PREVIOUS.*`) | `docker run -v "$(pwd):/zap/wrk:rw" zaproxy/zap-stable zap.sh -cmd -autorun /zap/wrk/zap.yaml` — config completa en [`sec/zap/zap.yaml`](sec/zap/zap.yaml) |
| Análisis estático SpotBugs/find-sec-bugs (233 hallazgos, 0 SQL dinámico — actualizado 2026-08-29) | [`sec/owasp/OWASP-AUDIT.md`](sec/owasp/OWASP-AUDIT.md), [`sec/static-analysis/STATIC-ANALYSIS.md`](sec/static-analysis/STATIC-ANALYSIS.md) | [`sec/static-analysis/spotbugs-findsecbugs-report-2026-08-29.xml`](sec/static-analysis/spotbugs-findsecbugs-report-2026-08-29.xml) (corrida 17-08 conservada como `spotbugs-findsecbugs-report.xml`) | `cd backend && ./mvnw com.github.spotbugs:spotbugs-maven-plugin:4.8.6.4:spotbugs`, filtro en `backend/spotbugs-security-include.xml` |
| `npm audit` (14 vulnerabilidades, 5 críticas — bajó de 41 tras actualizar `@angular/core` 21.2.12→21.2.22 el 2026-08-29) | [`sec/owasp/OWASP-AUDIT.md`](sec/owasp/OWASP-AUDIT.md) | Salida directa de `npm audit --json` (no versionada; es determinista mientras no cambie `Frontend/package-lock.json` ni la base de datos de advisories de npm) | `cd Frontend && npm audit` |
| Estado SUS (instrumento listo, sin datos reales aplicados) | [`sus/SUS-RESULTS.md`](sus/SUS-RESULTS.md) | N/A — no hay datos numéricos citables todavía; el archivo documenta por qué la puntuación de una versión anterior era fabricada | — |
| 31 controladores REST (actualizado 2026-09-11, antes 29, antes 25), 12 HU, 12 CU (SRS v1.0.1) | `README.md` | `docs/requisitos/SRS-v1.0.1.pdf`, `docs/requisitos/historias/`, `docs/requisitos/casos-de-uso/`, conteo de archivos en `backend/src/main/java/.../controllers/` | `find backend/src/main/java -iname "*Controller.java" \| wc -l` (usar el nombre de archivo, no `grep @RestController`: `GlobalExceptionHandler.java` usa `@RestControllerAdvice`, que un grep ingenuo cuenta como falso positivo). Nota: versiones anteriores del proyecto afirmaban "15 HUs, 15 CUs" sin que existieran — corregido en la Fase 7 a la cifra real (12), ver `docs/requisitos/CHANGELOG-REQ.md` |
| Unificación de cifras del entregable (examen suspenso P11, 2026-09-16): **31 controladores** y **10 rutinas SQL** son la única cifra vigente de cada una en todo el documento activo (`Informe-Final/`, `docs/requisitos/SRS-v1.0.1.tex`, `docs/basedatos/CATALOGO-SP.md`) | [`08-diseno-arquitectura.tex`](../../Informe-Final/secciones/08-diseno-arquitectura.tex), [`10-evaluacion-empirica.tex`](../../Informe-Final/secciones/10-evaluacion-empirica.tex), [`SRS-v1.0.1.tex`](../requisitos/SRS-v1.0.1.tex), [`CATALOGO-SP.md`](../basedatos/CATALOGO-SP.md) | `backend/src/main/java/.../controllers/*Controller.java` (31 archivos); `backend/src/main/resources/db/migration/V*.sql` (10 `CREATE PROCEDURE`/`CREATE FUNCTION` con nombre distinto) | Búsqueda reproducida: `grep -rniE "[0-9]+\s*controlador" Informe-Final/secciones/ docs/requisitos/SRS-v1.0.1.tex docs/mediciones/DATA-PROVENANCE.md` y `grep -rniE "[0-9]+\s*rutinas" Informe-Final/secciones/ docs/requisitos/SRS-v1.0.1.tex docs/basedatos/CATALOGO-SP.md` — toda mención en documentos activos da 31/10; los únicos valores distintos (`21 controladores` en `docs/mediciones/COVERAGE.md`, `25`/`29`/`30` en las filas OBS-13/14/19 de `docs/observaciones/OBSERVACIONES.md`, `49 controladores` en el propio SRS) aparecen en un documento explícitamente marcado "⚠️ Documento superado" o en narrativa de auto-corrección de un hallazgo pasado ("se creía X, la cifra real es 31"), nunca como una cifra vigente en conflicto |
| Versiones exactas del entorno (Docker, JDK, Node, Angular CLI, k6) | Este documento, informes | [`../entorno/versions.txt`](../entorno/versions.txt) | `docker --version`, `java -version`, `node --version`, `npx ng version`, `k6 version` (comandos documentados en `versions.txt`) |
| Volumen de datos (1,003,344 registros, 41 tablas) | [`../basedatos/VOLUMEN-DATOS.md`](../basedatos/VOLUMEN-DATOS.md) | [`../../scripts/generar-volumen-datos.sql`](../../scripts/generar-volumen-datos.sql) | `docker exec -i amz-postgres psql -U postgres -d BdPresustentaciones -v ON_ERROR_STOP=1 -f - < scripts/generar-volumen-datos.sql` sobre una BD recién migrada (solo catálogos base + usuarios demo). La versión anterior (1,010,242 filas) no tenía script versionado y no era reproducible — se perdió el 22 ago 2026 al recrear el volumen de Docker (ver `docs/observaciones/INFORME-ERRORES-2026-08-22.md`) |
| Auditoría de autorización en endpoints de escritura (102 endpoints, 0 sin `@PreAuthorize` salvo 5 exentos conocidos de `AuthController` — P8, examen suspenso 2026-09-16) | [`sec/owasp/OWASP-AUDIT.md`](sec/owasp/OWASP-AUDIT.md) (sección A05:2021) | [`sec/owasp/scripts/audit-endpoints-autorizacion.py`](sec/owasp/scripts/audit-endpoints-autorizacion.py) | `python docs/mediciones/sec/owasp/scripts/audit-endpoints-autorizacion.py` sobre `backend/src/main/java/.../controllers` |
| RF-61/RF-63/RNF-24 (respaldos): comandos y salidas reales de `pg_dump`/`pg_restore`/`pg_basebackup`, panel de estado/config/WAL, y el tiempo medido de una restauración completa sobre ~1,025,204 filas representativas (2026-09-11) | [`backup/RESPALDO-EVIDENCIA.md`](backup/RESPALDO-EVIDENCIA.md), [`backup/RESTAURACION-EVIDENCIA.md`](backup/RESTAURACION-EVIDENCIA.md), [`backup/RPO-RTO.md`](backup/RPO-RTO.md) | [`../../database/esquema.sql`](../../database/esquema.sql), [`../../database/datos_masivos.sql`](../../database/datos_masivos.sql) (commit `73c8902`, generador del dataset de ~1M usado para medir el RTO); respaldos reales generados vía la API durante la corrida (no versionados: dumps y bases físicas viven en el volumen `backend_uploads`/`postgres_wal`, no en git) | `docker compose up -d --build` + `curl` contra `POST/GET /api/v1/backups*` como ADMIN (comandos completos en los tres archivos de arriba); el dataset de 1M se generó y restauró en bases **aisladas** (`BdPresustentaciones_1M`, `..._restore`, `presusDb_prueba_restauracion`), nunca sobre `BdPresustentaciones`, y se eliminaron al terminar |

## Nota sobre reproducibilidad

Los archivos JSON/CSV/XML de este documento que están versionados en el repositorio (`k6/run*.json`, `perf/lighthouse/prod-runs/*.json`, `sec/zap/*.json`, `sec/static-analysis/*.xml`) son **snapshots de una corrida real en una fecha concreta**, no generados en cada build. Los que dependen del estado exacto de la máquina donde se corrieron (coverage, npm audit, resultados de tests) se regeneran corriendo el comando indicado — el CI en [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) los regenera en cada push para el caso de JaCoCo y SpotBugs.

## Parámetros, semilla, versión y commit — nota sobre la cadena completa de trazabilidad

- **Parámetros:** cada fila de la tabla ya declara el comando completo con sus flags reales (p. ej.
  `--preset=desktop`, `--summary-export=runN-summary.json`) — no hay parámetros ocultos no documentados.
- **Semilla (seed):** la mayoría de este pipeline no usa generación aleatoria que necesite semilla fija
  para ser reproducible — `scripts/generar-volumen-datos.sql` usa `generate_series()` determinista (sin
  `random()`), y las muestras de k6/Lighthouse/cache fría-caliente son mediciones empíricas reales, no
  simulaciones muestreadas. Excepción honesta: `database/datos_masivos.sql` (usado para la evidencia de
  RNF-24 de arriba) sí llama `random()` dos veces (líneas de generación de hashes md5 de archivos
  ficticios), sin `setseed()`; no es determinista fila por fila, pero tampoco importa para lo que mide esa
  evidencia — el volumen total de filas por tabla es fijo (`generate_series` con límites fijos en la
  sección `_cfg`), y el tiempo de `pg_dump`/`pg_restore` depende del volumen y del esquema, no del
  contenido aleatorio de esas dos columnas de texto.
- **Versión / commit:** las versiones exactas de las herramientas usadas están en
  [`../entorno/versions.txt`](../entorno/versions.txt) (regenerable con `scripts/gen-versions.sh`). El
  commit exacto del código que generó cada snapshot archivado corresponde al `HEAD` de `main` en la fecha
  indicada en cada fila — **con una salvedad honesta:** las correcciones y evidencia generadas durante la
  auditoría de reproducibilidad de 2026-08-29/30 (incluida esta misma tabla) todavía no estaban
  commiteadas al momento de escribirse, así que no se cita un hash de commit específico para ellas —
  citar uno inventado violaría el principio de este mismo documento. Una vez commiteadas, el hash real
  queda disponible vía `git log` para quien necesite ese nivel de detalle.
